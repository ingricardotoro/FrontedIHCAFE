/*import React, { Component } from "react";

export default class Test extends Component {
  render() {
    const {name, edad } = this.props
    Test.defaultProps = {
       name:"Name_default",
       edad:0
    }
    return (
      <div>
        <h2> "Nombre" + {name} </h2>
        <h2> "Edad" + {edad} </h2>
      </div>
    );
  }
}

export default class Test extends Component {
    constructor(props){
        super(props)
        this.state = { numeros : [1,2,3,4,5] } 
    }
    render() {
      return (
        <div>
           {this.state.numeros.map(num => {
               return <p key ={id} >el numero es {num} </p>
           })}
        </div>
      );
    }
  }



<Test name="Ricardo" edad={30} Aprobado={true} arregloDeNumero={[1,2,3]} />;

<Test objetoAB = {{Key1 = "Valor1"},{Key2 = "Valor2"} } />;

<Test multiplicar= {(valor) => valor * 3 } />; 

<Test titulo= { <h1> Elemento h1 </h1> } />; */
